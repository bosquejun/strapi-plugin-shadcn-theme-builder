import activeTheme from './active-theme/schema.json';
import theme from './themes/schema.json';

export default {
  'active-theme': {
    schema: activeTheme,
  },
  theme: {
    schema: theme,
  },
};
